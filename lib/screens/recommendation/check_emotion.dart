import 'package:flutter/material.dart';

class CheckEmotionPage extends StatelessWidget {
  const CheckEmotionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('1 페이지'),
      ),
    );
  }
}
